import { useAppContext } from "../../contexts/AppContext"


export const PostSeasonDisplay = () => {
    const { selectedLeague } = useAppContext()

    

    // draft list
    // player points per week (all weeks, including penalties)
    // smart player points (only valid weeks)

    // other stats might include biggest blowout, draft grades

    // if (selectedLeague)
    // I will need to include IsLeagueFinished in the export DTO so it can be checked

    return <div>bubkis</div>
}