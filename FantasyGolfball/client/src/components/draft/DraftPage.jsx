import { useAppContext } from "../../contexts/AppContext"


export const DraftPage = () => {
    const { players } = useAppContext()

    // I will need a mockup of this UI before I do anything to actually design it.
    return (
        <div></div>
    )
}