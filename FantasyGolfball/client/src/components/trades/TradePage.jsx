

export const TradePage = () => {

    // display a notification icon on the navbar IF any unresolved trades WHERE user not the proposer
    // OR both-accepted unresolved trades WHERE user is the proposer

    return (
        <div>Collapsable list of proposed trades</div>
    )
}